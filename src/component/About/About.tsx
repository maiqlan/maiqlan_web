import React from "react";
import { getImageUrl } from '../../utils';

export const About = () => {
    return (
        <section>
            <div>
                
            </div>
        </section>
    );
}